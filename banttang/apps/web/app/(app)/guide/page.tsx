// 반띵 가이드 — Lovable "모아 파티 가이드" 디자인을 반띵 톤/모델로 포팅.
// 인터랙션(탭·FAQ·공유)이 많아 화면 전체를 클라이언트 컴포넌트로 둔다.

import { GuideScreen } from "@/components/guide/guide-screen";

export default function TradeGuidePage() {
  return <GuideScreen />;
}
