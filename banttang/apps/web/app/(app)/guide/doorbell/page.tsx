// 띵동이란? 가이드 — Lovable "dingdong" 디자인을 반띵 톤/실제 동작으로 포팅.
// 인터랙션(FAQ 아코디언)이 있어 화면 전체를 클라이언트 컴포넌트로 둔다.

import { DoorbellGuideScreen } from "@/components/guide/doorbell-guide-screen";

export default function DoorbellGuidePage() {
  return <DoorbellGuideScreen />;
}
