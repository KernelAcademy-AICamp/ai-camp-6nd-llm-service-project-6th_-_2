import { OnboardingTour } from "@/components/onboarding/OnboardingTour";

// UI-only 단계 — 챗봇 흐름만 동작. 데이터 저장/리다이렉트 가드는 추후 추가.
export default function OnboardingTourPage() {
  return <OnboardingTour />;
}
