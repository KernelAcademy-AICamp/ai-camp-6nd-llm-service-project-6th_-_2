-- ============================================================================
-- 반띵 — 핫딜 출처 URL 컬럼 추가
-- ============================================================================
--
-- 배경: 커뮤니티 RSS 목록엔 원문 쇼핑몰 링크가 없고, 글 본문에만 "출처 : <url>"
--       형태로 들어있다(루리웹 div.source_url 등). 크롤러가 신규 글 상세를
--       1회 더 fetch해 추출 → 여기 저장. 카드에서 출처(쇼핑몰)로 표시.
--   docs/hotdeal-aggregator.md
-- ============================================================================

ALTER TABLE hotdeals ADD COLUMN IF NOT EXISTS source_url text;

COMMENT ON COLUMN hotdeals.source_url IS '원문 글 본문에서 추출한 실제 쇼핑몰 URL(있을 때만).';

NOTIFY pgrst, 'reload schema';
